import hashlib

# Here's an example that shows the syntax for calculating an md5 hash:
# plainText = "Hello World"
# hashText = hashlib.md5(plainText.encode()).hexdigest()

DICT = "/usr/share/dict/american-english"
HASHES = "hashes.txt"
MAXLINES = 10
